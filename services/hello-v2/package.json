{
  "name": "@my-app/hello-v2",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@my-app/logger": "workspace:*",
    "@my-app/middleware": "workspace:*"
  },
  "scripts": {
    "lint": "npx eslint {src,test}/**/*.{ts,tsx,json,yml}",
    "start": "npx sls offline start --host localhost",
    "test": "npx jest",
    "tsc": "npx tsc",
    "prune": "npx sls prune -n 3",
    "create-domain": "npx sls create_domain",
    "package": "npx sls package",
    "deploy": "npx sls deploy"
  }
}
