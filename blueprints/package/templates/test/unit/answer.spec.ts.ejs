import { computeAnswerToUniverse } from '../../src';

describe('The almighty helper function', () => {
  it('should compute answer to universe and everything', async () => {
    expect(computeAnswerToUniverse()).toBe(42);
  });
});
